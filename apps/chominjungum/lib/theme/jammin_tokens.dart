import 'package:flutter/material.dart';

/// jammin `jammin-tokens.css` 와 동일한 브랜드·표면 색.
abstract final class JamminTokens {
  static const brand = Color(0xFF296429);
  static const brandDark = Color(0xFF1E4A1E);
  static const brandMid = Color(0xFF357A35);
  static const brandLight = Color(0xFF5F9A5F);
  static const brandMuted = Color(0xFF7AAD7A);
  static const brandSoft = Color(0x24296429);

  static const surface = Color(0xFFF8FAFC);
  static const surfaceElevated = Color(0xFFFFFFFF);
  static const text = Color(0xFF0F172A);
  static const textMuted = Color(0xFF64748B);
  static const border = Color(0xFFE2E8F0);
  static const borderStrong = Color(0xFFCBD5E1);

  static const navGuideFg = Color(0xFFD1FAE5);
  static const accent = Color(0xFFD97706);
  static const danger = Color(0xFFB91C1C);
  static const success = Color(0xFF1B6E32);
  static const successHover = Color(0xFF145226);

  static const pageMaxWidth = 1140.0;
  static const pagePaddingH = 20.0;
  static const sectionPaddingTop = 48.0;
  static const sectionPaddingBottom = 32.0;
  static const radiusSm = 6.0;
  static const radiusMd = 8.0;
  static const radiusLg = 16.0;
}
